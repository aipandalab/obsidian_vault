
---

**1. 我的学科平均分**
* **挑战描述:** 小明刚考完三门摸底考试，分别是语文、数学和英语。请你帮他计算一下这三门课的总分和平均分（平均分结果保留一位小数）。
* **输入格式:** 一行，包含三个用空格隔开的整数（0-100范围），代表三门课的成绩。
* **输出格式:** 两行。第一行输出总分。第二行输出平均分，保留一位小数。
* **样例输入:** `85 92 78`
* **样例输出:**
    ```
    255
    85.0
    ```

**2. 电影/游戏分级检查**
* **挑战描述:** 某款热门游戏或电影有年龄限制，需要满12岁才能玩/看。请编写程序，输入一个用户的年龄，判断他/她是否符合要求。
* **输入格式:** 一个正整数，代表用户的年龄。
* **输出格式:** 如果年龄大于等于12，输出 `Allowed`，否则输出 `Restricted`。
* **样例输入:** `15`
* **样例输出:** `Allowed`
* **样例输入:** `10`
* **样例输出:** `Restricted`

**3. 数学谜题：阶乘计算**
* **挑战描述:** 小明在解一道组合数学题时，需要快速计算一个数字 $N$ 的阶乘 ($N! = 1 \times 2 \times \dots \times N$)。你能帮他写个程序吗？（记住哦，$0! = 1$）
* **输入格式:** 一个非负整数 $N$ ($0 \le N \le 15$)。
* **输出格式:** 一个整数，表示 $N!$ 的结果。
* **样例输入:** `6`
* **样例输出:** `720`

**4. 用户名复杂度分析**
* **挑战描述:** 注册一个网站账号时，通常建议用户名包含字母、数字等。请写程序分析一个给定的用户名（字符串），统计其中英文字母、数字和其他符号（如!@#$等）各有多少个。
* **输入格式:** 一行字符串，代表用户名。
* **输出格式:** 三行，每行一个整数，分别表示英文字母、数字和其他符号的个数。
* **样例输入:** `Gamer_123!#`
* **样例输出:**
    ```
    5
    3
    2
    ```

**5. 一周气温极值**
* **挑战描述:** 记录了一周（7天）的每日最高气温。请找出这一周气温的最高值和最低值。
* **输入格式:** 一行，包含7个用空格隔开的整数，代表每日最高气温。
* **输出格式:** 一行，包含两个用空格隔开的整数，分别是最高气温和最低气温。
* **样例输入:** `25 28 22 30 29 26 24`
* **样例输出:** `30 22`

**6. 数学社团的“质数”挑战**
* **挑战描述:** 学校数学社团正在研究质数（素数）。请编写一个函数 `is_prime(n)` 判断输入的正整数 $n$（$n > 1$）是否是质数。主程序读入 $n$，调用函数并输出判断结果。
* **输入格式:** 一个大于1的正整数 $n$。
* **输出格式:** 如果 $n$ 是质数，输出 `Prime Number`，否则输出 `Composite Number`。
* **样例输入:** `13`
* **样例输出:** `Prime Number`
* **样例输入:** `12`
* **样例输出:** `Composite Number`

**7. 模拟兔子繁殖（斐波那契）**
* **挑战描述:** 有趣的斐波那契数列可以模拟理想状态下兔子的繁殖（或其他增长模式）。假设第1个月有1对兔子，第2个月还是1对，从第3个月开始，每月的兔子对数等于前两个月对数之和。输入月份 $N$，输出前 $N$ 个月（包括第N个月）的兔子对数。
* **输入格式:** 一个正整数 $N$ ($1 \le N \le 30$)。
* **输出格式:** 一行，包含前 $N$ 个月的兔子对数，数字间用空格隔开。
* **样例输入:** `7`
* **样例输出:** `1 1 2 3 5 8 13`

**8. 秘密口令反转**
* **挑战描述:** 小华和小伙伴玩传话游戏，他们约定把一句话里的单词顺序反过来说作为“秘密口令”。请帮他们实现这个口令生成器。
* **输入格式:** 一行字符串，包含英文单词和空格。
* **输出格式:** 一行字符串，单词顺序反转后的口令。
* **样例输入:** `secret message here`
* **样例输出:** `here message secret`

**9. 购物清单去重**
* **挑战描述:** 小明列了一个购物清单，但有些物品不小心写了好几遍。请你帮他整理一下，去掉重复的物品，每种物品只保留第一次出现的那个。
* **输入格式:** 一行，包含若干用空格隔开的物品名称（字符串）。
* **输出格式:** 一行，包含去重后的物品清单，物品间用空格隔开，保持原出现顺序。
* **样例输入:** `apple banana orange apple grape banana`
* **样例输出:** `apple banana orange grape`

**10. 单词字母连连看**
* **挑战描述:** 对一个给定的英文单词（全小写），统计其中每个字母出现的次数，并按字母表顺序输出。看看哪个字母用得最多？
* **输入格式:** 一行字符串，代表一个英文单词（全小写）。
* **输出格式:** 若干行，每行格式为 `字母:次数`。按字母顺序输出。
* **样例输入:** `programming`
* **样例输出:**
    ```
    a:1
    g:2
    i:1
    m:2
    n:1
    o:1
    p:1
    r:2
    ```

**11. 神奇的回文词**
* **挑战描述:** "level"、"madam" 这些词正着读反着读都一样，它们被称为回文词。请写程序判断一个输入的单词是否是回文词。
* **输入格式:** 一行字符串。
* **输出格式:** 如果是回文词，输出 `Palindrome`，否则输出 `Not Palindrome`。
* **样例输入:** `racecar`
* **样例输出:** `Palindrome`
* **样例输入:** `hello`
* **样例输出:** `Not Palindrome`

**12. 完美铺砖块 (最大公约数应用)**
* **挑战描述:** 要用同样大小的正方形瓷砖铺满一个长 $a$ 厘米、宽 $b$ 厘米的矩形区域，且不能切割瓷砖。为了让瓷砖尽可能大（从而用的数量最少），应该选择边长是多少厘米的瓷砖？（提示：这其实是在求 $a$ 和 $b$ 的最大公约数）
* **输入格式:** 一行，包含两个用空格隔开的正整数 $a$ 和 $b$ ($1 \le a, b \le 10000$)。
* **输出格式:** 一个整数，表示最大允许的正方形瓷砖边长。
* **样例输入:** `36 24`
* **样例输出:** `12`

**13. 堆叠积木**
* **挑战描述:** 用 `*` 代表积木块，请按照要求堆叠一个 $N$ 层高的、底边靠左的直角三角形积木堆。
* **输入格式:** 一个正整数 $N$ ($1 \le N \le 10$)。
* **输出格式:** $N$ 行 `*` 组成的积木堆。
* **样例输入:** `5`
* **样例输出:**
    ```
    *
    **
    ***
    ****
    *****
    ```

**14. 图书馆找书**
* **挑战描述:** 图书馆有一排书架，编号分别是列表中的数字。输入 $N$ 个书架编号，以及你要找的书所在的特定书架编号 $X$，请告诉我这本书在列表中的哪个位置（索引，从0开始）。如果图书馆没有这个编号的书架，就告诉我找不到（输出-1）。
* **输入格式:**
    * 第一行：书架总数 $N$ ($1 \le N \le 100$)。
    * 第二行：$N$ 个用空格隔开的正整数，代表书架编号。
    * 第三行：要查找的书架编号 $X$。
* **输出格式:** 一个整数，表示 $X$ 在列表中的第一个索引，或者 -1。
* **样例输入:**
    ```
    6
    101 203 105 203 310
    203
    ```
* **样例输出:** `1`
* **样例输入:**
    ```
    4
    101 102 103 104
    105
    ```
* **样例输出:** `-1`

**15. 运动会成绩排名**
* **挑战描述:** 校运动会某项比赛结束，记录了 $N$ 名选手的得分。请将这些得分从低到高排序输出。
* **输入格式:**
    * 第一行：选手人数 $N$ ($1 \le N \le 100$)。
    * 第二行：$N$ 个用空格隔开的整数，代表选手得分。
* **输出格式:** 一行，包含排序后的 $N$ 个得分，用空格隔开。
* **样例输入:**
    ```
    5
    88 75 92 80 75
    ```
* **样例输出:** `75 75 80 88 92`

**16. 班级平均分与优秀人数**
* **挑战描述:** 老师录入了全班 $N$ 位同学的某次测试成绩（0-100分）。请计算班级的平均分（保留一位小数），并统计有多少同学的分数超过或等于了平均分？
* **输入格式:**
    * 第一行：学生人数 $N$ ($1 \le N \le 100$)。
    * 第二行：$N$ 个用空格隔开的整数，代表学生成绩。
* **输出格式:**
    * 第一行：平均分，保留一位小数。
    * 第二行：分数高于或等于平均分的人数。
* **样例输入:**
    ```
    6
    80 95 70 85 60 75
    ```
* **样例输出:**
    ```
    77.5
    4
    ```

**17. 模拟掷骰子**
* **挑战描述:** 玩桌面游戏经常要掷骰子。假设有一个标准的六面骰子（点数1到6）。请模拟掷 $N$ 次骰子，计算并输出 $N$ 次掷出的点数总和。（提示：使用 `random.randint(1, 6)`）
* **输入格式:** 一个正整数 $N$ ($1 \le N \le 100$)，表示掷骰子的次数。
* **输出格式:** 一个整数，表示 $N$ 次点数的总和。(注意：每次运行结果可能不同)
* **样例输入:** `3`
* **样例输出 (仅为示例):** `11` (例如掷出了 5, 2, 4)

**18. 班长选举计票**
* **挑战描述:** 班级正在选举班长，同学们投了 $N$ 票。每张票上写着候选人的名字。请统计每位候选人最终获得了多少票，并按候选人名字的字典序输出结果。
* **输入格式:**
    * 第一行：投票总数 $N$ ($1 \le N \le 100$)。
    * 接下来 $N$ 行，每行是一个候选人的名字（字符串，无空格，区分大小写）。
* **输出格式:** 若干行，每行格式为 `候选人名字:票数`。按名字的字典序输出。
* **样例输入:**
    ```
    7
    LiMing
    ZhangHua
    LiMing
    WangFang
    ZhangHua
    LiMing
    ZhaoWei
    ```
* **样例输出:**
    ```
    LiMing:3
    WangFang:1
    ZhangHua:2
    ZhaoWei:1
    ```

**19. 零花钱账本统计**
* **挑战描述:** 小明把每天的零花钱支出记在了一个文件 `expenses.txt` 里，每天的支出占一行。请读取这个文件，计算小明总共支出了多少钱。假设文件一定存在且每行只有一个数字（可以是整数或带小数的数）。
* **`expenses.txt` 文件内容示例:**
    ```
    5.5
    10
    3.75
    8
    ```
* **输入格式:** 无（程序直接读取 `expenses.txt` 文件）。
* **输出格式:** 一个数字，表示总支出金额。
* **样例输出 (基于上述文件内容):** `27.25`

**20. 找出日记里最长的英文单词**
* **挑战描述:** 小红写了一篇英文日记（一个句子）。请找出这句日记中字母最多的那个单词。如果最长的单词不止一个，输出第一个出现的那个。
* **输入格式:** 一行字符串，表示一句英文日记（仅含字母和空格）。
* **输出格式:** 一行字符串，表示句子中最长的单词。
* **样例输入:** `Today was a beautiful and unforgettable day`
* **样例输出:** `unforgettable`
* **样例输入:** `Practice makes perfect`
* **样例输出:** `Practice` (如果长度相同，输出第一个)

**21. 判断完美数**

- **挑战描述:** 一个数如果恰好等于它的真因子（即除了自身以外的约数）之和，这个数就称为完美数。例如 6 的真因子是 1, 2, 3，且 1+2+3 = 6。输入一个正整数 N，判断它是否是完美数。
- **输入格式:** 一个正整数 N (1<N≤10000)。
- **输出格式:** "Yes" 或 "No"。
- **样例输入:** `6`
- **样例输出:** `Yes`
- **样例输入:** `28`
- **样例输出:** `Yes`
- **样例输入:** `10`
- **样例输出:** `No`

**22.凯撒密码加密**

- **挑战描述:** 凯撒密码是一种简单的替换加密技术，将字母表中的每个字母向后（或向前）移动固定数量的位置。输入一个仅包含小写字母的单词和一个偏移量 K (正整数)，将单词加密后输出。（只对字母加密，循环移动，例如 'z' 偏移 1 变成 'a'）
- **输入格式:** 两行。第一行是单词，第二行是偏移量 K (1≤K≤25)。
- **输出格式:** 加密后的单词。
- **样例输入:**
    
    ```
    hello
    3
    ```
    
- **样例输出:** `khoor`

**23. 队列模拟：排队与出队**

- **挑战描述:** 模拟一个简单的排队过程。输入 N 个按顺序入队的人名。然后输入一个数字 M，表示有 M 个人出队。输出出队 M 个人后，仍在队列中的人名（按原顺序）。
- **输入格式:**
    - 第一行：一个正整数 N。
    - 接下来 N 行，每行一个入队人名。
    - 最后一行：一个整数 M (0≤M≤N)。
- **输出格式:** 若干行，每行一个仍在队列中的人名（按原入队顺序）。如果队列为空，则不输出。
- **样例输入:**
    
    ```
    5
    Alice
    Bob
    Charlie
    David
    Eve
    2
    ```
    
- **样例输出:**
    
    ```
    Charlie
    David
    Eve
    ```